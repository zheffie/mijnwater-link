[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    live: tests that hit the real Water-link portal (require credentials; run with: pytest -m live -s)
; live tests are excluded by default; run them explicitly with: pytest -m live -s
addopts = -m "not live"
